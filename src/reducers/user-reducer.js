const INITIAL_STATE = {
  email: 'ahmet.ddu@gmail.com',
  name: 'ahmet',
  surname: 'aydogdu',
};

export default (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
